<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dduplacement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Registered_company_Details"></activity>
        <activity android:name=".Company_Details_Activity" />
        <activity android:name=".Registered_Company" />
        <activity android:name=".Profile" />
        <activity android:name=".Forgot_password" />
        <activity android:name=".Signup_form" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Login_form">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>